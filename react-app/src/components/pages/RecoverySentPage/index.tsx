import React from "react";
import './index.css';

const RecoverySentPage: React.FC = () => (
  <div className="recovery-sent-page">
    If an email matches an account, a password reset email will be sent to the associated email address with instructions to reset your password.
  </div>
)

export default RecoverySentPage;